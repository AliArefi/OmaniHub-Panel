# In local dev, the app defaults to `/api/1` and Vite proxies it to `https://admin.omanihub.com` (see `vite.config.ts`).
# Override if you want to target a different backend.
VITE_API_PREFIX=/api/1

VITE_URL_IMAGE=

VITE_ENABLE_MOCK=false
